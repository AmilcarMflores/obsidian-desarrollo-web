Esta sintaxis, es considerada la forma estándar y "profesional" de escribir código asíncrono hoy en día.
## Async / Await (Azúcar Sintáctico)
Es una capa visual que "endulza" o facilita la escritura de Promesas para que el código asíncrono se lea y se comporte visualmente como si fuera síncrono (secuencial de arriba a abajo).

**Las dos palabras clave:**
1. `async:` Se coloca antes de declarar una función (ej: `async function`).
	- Convierte automáticamente la función en una promesa.
	- Habilita el uso de `await` dentro de su cuerpo.
2. `await`: Se coloca antes de una llamada que devuelve una promesa.
	- **Pausa** la ejecución de esa función específica (no congela el navegador) hasta que la promesa se resuelve.
	- Extrae el valor resuelto de la promesa y lo asigna a una variable, eliminando la necesidad de los `.then()`. 

**Escenario:** Queremos pedir un usuario, esperar a que llegue, y mostrarlo.
```javascript
// --- FUNCIÓN QUE SIMULA UNA API (Devuelve una Promesa) ---
function pedirUsuario() {
  return new Promise((resolve) => {
    setTimeout(() => resolve({ id: 1, nombre: "Jhon" }), 1000);
  });
}

// --- ESTILO 1: Promesas Clásicas (.then) ---
// Se lee un poco "desordenado" por las funciones flecha anidadas
function verUsuarioPromesa() {
  pedirUsuario()
    .then((usuario) => {
      console.log("Usuario recibido:", usuario);
    })
    .catch((error) => console.error(error));
}

// --- ESTILO 2: Async / Await (Moderno) ---
// Se lee secuencialmente, como si fuera código bloqueante
async function verUsuarioAsync() {
  try {
    console.log("Esperando datos...");
    
    // Aquí el código se "pausa" 1 segundo, pero el navegador sigue vivo.
    // El valor resuelto se guarda directamente en la constante.
    const usuario = await pedirUsuario();
    
    console.log("Usuario recibido:", usuario);
  } catch (error) {
    // Manejamos los errores (reject) con un bloque try-catch estándar
    console.error("Hubo un error:", error);
  }
}

verUsuarioAsync();
```
**Otro ejemplo más completo:** 
```javascript
// 1. La función auxiliar (simula el servidor)
function descargarDatos() {
  return new Promise((resolve, reject) => {
    const exito = true; // Cambiar a false para probar el catch
    
    setTimeout(() => {
      if (exito) {
        resolve("📦 Datos descargados correctamente");
      } else {
        reject("❌ Error en la conexión");
      }
    }, 1500);
  });
}

// 2. La función Asíncrona (El consumidor)
// Nota: Se usa 'async' antes de la declaración
const funcionAsincrona = async () => {
  try {
    console.log("1. Iniciando petición...");

    // El 'await' espera a que la promesa 'descargarDatos' termine.
    // Si es exitosa, guarda el mensaje en 'respuesta'.
    // Si falla, salta inmediatamente al bloque 'catch'.
    const respuesta = await descargarDatos();
    
    console.log(`2. Respuesta del servidor: ${respuesta}`);
    console.log("3. Procesando datos...");

  } catch (error) {
    // Aquí atrapamos cualquier error que ocurra arriba (el reject)
    console.error(`Error capturado: ${error}`);
  } finally {
    console.log("4. Fin del proceso (Se ejecuta siempre)");
  }
};

// Ejecutamos
funcionAsincrona();
```
## Notas
- **Regla de Oro:** Solo se puede usar `await` dentro de una función que tenga `async`. Si intentas usar `await` en el nivel superior de un archivo (fuera de una función) en versiones viejas de Node o navegadores antiguos, dará error (aunque las versiones más nuevas ya soportan "Top-level await").
- **Manejo de Errores:** En las promesas con `.then()`, es fácil olvidar poner el `.catch()`. En `async/await`, el uso de `try...catch` es más natural y robusto, ya que permite manejar errores de la promesa y errores de sintaxis en el mismo bloque.